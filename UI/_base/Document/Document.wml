<Router.Controller>
   <ws:partial template="{{application}}" scope="{{_options}}"
               on:themeChanged="setTheme()"
               on:changeApplication="changeApplicationHandler()"
               theme="{{coreTheme}}"/>
</Router.Controller>
