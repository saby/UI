<UI._base.RouteCompatible
   application="{{application}}"
   scope="{{_options}}"
   attr:key="bd_"
   wsRoot="{{ wsRoot }}"
   cssLinks="{{ cssLinks }}"
   jsLinks="{{ jsLinks }}"
   staticDomains="{{staticDomains}}"
   cssBundles="{{ cssBundles }}"
   servicesPath="{{ servicesPath }}"/>
