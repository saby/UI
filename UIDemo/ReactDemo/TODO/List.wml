<ws:if data="{{_options.items}}">
   <ul>
      <ws:for data="i in _options.items">
         <UIDemo.ReactDemo.TODO.Item
               key="{{i.id}}"
               title="{{i.title}}"
               removeHandler="{{_options.removeHandler.bind(undefined, i.id)}}"/>
      </ws:for>
   </ul>
</ws:if>
<ws:else>
   Список пуст
</ws:else>